{
  "name": "mongo",
  "version": "1.0.0",
  "main": "index.mjs",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.19.0",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "express-enqueue": "^1.0.0",
    "mongoose": "5.9.10"
  },
  "engines": {
    "node": "10.x"
  },
  "scripts": {
    "server-feed": "nodemon server.mjs",
    "deploy": "now",
    "gitHub": "git status && git add -A && git commit -am 'update' && git push",
    "kill;3004": "kill $(lsof -t -i:3004)"
  },
  "devDependencies": {
    "nodemon": "^2.0.3"
  }
}
